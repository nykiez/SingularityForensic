<UserControl x:Class="Singularity.UI.Controls.Views.ActiveOrNotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Singularity.UI.Controls.Views"
             mc:Ignorable="d" 
              d:DesignWidth="360">
    <Border BorderThickness="1" Padding="0,3" >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="12"/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="1">
                <Button x:Name="btnAct" Click="btnAct_Click"  Content="{DynamicResource Activate}" Margin="0,0,12,0" />
                <Button x:Name="btnCancel" Click="btnCancel_Click" Style="{StaticResource DenyBtnStyle}" Content="{DynamicResource Cancel}" />
                <Button x:Name="btnDeact" Click="btnDeact_Click" Style="{DynamicResource DeActiveBtn}" Content="{DynamicResource Deactivate}" DockPanel.Dock="Right"/>
                <Border/>
            </DockPanel>
        </Grid>
    </Border>
</UserControl>
