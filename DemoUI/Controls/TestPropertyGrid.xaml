<UserControl x:Class="DemoUI.Controls.TestPropertyGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <telerik:RadPropertyGrid IsGrouped="True" Item="{Binding Item}" LabelColumnWidth="180" DescriptionPanelVisibility="Collapsed" SelectedPropertyDefinition="{Binding SelectedProperty,Mode=TwoWay}" >
          
        </telerik:RadPropertyGrid>
    </Grid>
</UserControl>
